# Antique Items Client

React + TypeScript + Vite, with React Compiler enabled.

## Development

Run these commands from the client directory:

~~~sh
npm install
npm run dev
~~~

## Checks and production build

~~~sh
npm run lint
npm run build
npm run preview
~~~

Start building the UI in src/App.tsx. Global styles are in src/index.css.
